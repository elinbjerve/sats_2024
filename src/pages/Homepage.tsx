import React from "react";
import ListView from "../components/ListView";

const Homepage: React.FC = () => {
    return (
        <div>
            <h1>SATS gruppetimer</h1>
            <ListView /> 
        </div>
    );
};

export default Homepage;
